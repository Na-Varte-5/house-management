# =============================
# Global / Workspace
# =============================
# Rust build output (workspace + any nested crates like api/, frontend/)
**/target/

# Backup / temporary Rust files
**/*.rs.bk

# Cargo incremental build cache artifacts (already inside target, but just in case)
.cargo/registry/
.cargo/git/

# =============================
# Environment & Secrets
# =============================
.env
.env.*
!env.example
# If you keep a sample env file, name it env.example and it will be kept.

# Diesel CLI autogenerated config can be committed (diesel.toml) so we don't ignore it.
# Ignore potential local database dump or scratch SQL files
*.sql
!migrations/**/*.sql

# =============================
# Frontend (Yew + Trunk)
# =============================
# Trunk build output (default dist directory when running trunk build or serve)
**/dist/
# Trunk staging directory
**/.trunk/

# wasm-pack (if ever used directly)
**/pkg/

# WebAssembly artifacts that might be copied outside target (defensive)
*.wasm
!frontend/static/**/*.wasm

# Sass / CSS tooling caches
.sass-cache/
*.css.map

# =============================
# Logs & Runtime
# =============================
*.log
logs/

# Coverage / profiling
coverage/
htmlcov/
.profraw
*.profdata

# =============================
# Editors & IDEs
# =============================
.idea/
.vscode/
*.swp
*.swo
*.iml

# VS Code Rust analyzer cache (optional)
.rust-analyzer*

# =============================
# OS / Filesystem Noise
# =============================
.DS_Store
Thumbs.db

# =============================
# Node / JS tooling (in case auxiliary tooling is added later)
# =============================
node_modules/
*.tsbuildinfo

# =============================
# Miscellaneous
# =============================
# Local generated assets / caches
.cache/
.tmp/
/tmp/

# Remove if you intentionally add local binary utilities
bin/

# Ignore potential per-developer HTTP client scratch files except the committed one
# (We keep rest-api.http that you already have.)
*.http
!rest-api.http

# Translation build artifacts (keep source .ftl files, ignore compiled caches if any)
*.mo

# =============================
# Prevent ignoring desirable files
# =============================
!**/migrations/**
!**/locales/**
!**/locales/**/*.ftl

# End of .gitignore
